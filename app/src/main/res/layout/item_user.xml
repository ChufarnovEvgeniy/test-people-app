<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/main_margin"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/name_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            android:textSize="@dimen/large_text_size"
            tools:text="Aisha Velasquez" />

        <TextView
            android:id="@+id/email_text_view"
            style="@style/TextAppearance.AppCompat.Subhead"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/medium_text_size"
            tools:text="aishavelasquez@bugsall.com" />

    </LinearLayout>

    <ImageView
        android:id="@+id/active_image_view"
        android:layout_width="@dimen/active_image_size"
        android:layout_height="@dimen/active_image_size"
        android:layout_gravity="center"
        android:layout_margin="@dimen/main_margin"
        app:srcCompat="@drawable/ic_active"
        app:tint="@color/green_active_color" />

</LinearLayout>